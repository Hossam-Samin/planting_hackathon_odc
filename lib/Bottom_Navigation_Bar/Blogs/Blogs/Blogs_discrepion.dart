

import 'package:flutter/material.dart';

class Description extends StatelessWidget {
  const Description({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Scaffold(
        body: ListView(
          children: [
            Column(
              children: [
                const SizedBox(height: 30,),
                
                Container(
                  height: 300,
                  width: 300,
                  child: Image.asset("images/Rectangle 15.png",fit: BoxFit.contain,),
                ),

               const SizedBox(height: 20,),
              const Text("5 Simple Tips to treat plants",style: TextStyle(fontSize: 25,fontWeight: FontWeight.bold),),
                const SizedBox(height: 10,),
               const Text(
                "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey,),textAlign: TextAlign.center,),
              const SizedBox(height: 5,),
                const Text(
                  "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                  "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                  "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                  "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                  "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),



              ],
            )
          ],
        ),
      ),
    );
  }
}

class Descripion2 extends StatelessWidget {
  const Descripion2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Scaffold(
        body: ListView(
          children: [
            Column(
              children: [
                const SizedBox(height: 30,),

                Container(
                  height: 300,
                  width: 300,
                  child: Image.asset("images/stephanie-harvey-vHkj3fX9wCk-unsplash 2.png",fit: BoxFit.contain,),
                ),

                const SizedBox(height: 20,),
                const Text("5 Simple Tips to treat plants",style: TextStyle(fontSize: 25,fontWeight: FontWeight.bold),),
                const SizedBox(height: 10,),
                const Text(
                  "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey,),textAlign: TextAlign.center,),
                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),



              ],
            )
          ],
        ),
      ),
    );
  }
}

class Descripion3 extends StatelessWidget {
  const Descripion3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Scaffold(
        body: ListView(
          children: [
            Column(
              children: [
                const SizedBox(height: 30,),

                Container(
                  height: 300,
                  width: 300,
                  child: Image.asset("images/Mask group.png",fit: BoxFit.contain,),
                ),

                const SizedBox(height: 20,),
                const Text("5 Simple Tips to treat plants",style: TextStyle(fontSize: 25,fontWeight: FontWeight.bold),),
                const SizedBox(height: 10,),
                const Text(
                  "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey,),textAlign: TextAlign.center,),
                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),

                const SizedBox(height: 5,),
                const Text(
                    "leaf, in botany, any usually flattened green outgrowth from the stem of",style: TextStyle(fontSize: 20,color: Colors.grey),textAlign: TextAlign.center),



              ],
            )
          ],
        ),
      ),
    );
  }
}


